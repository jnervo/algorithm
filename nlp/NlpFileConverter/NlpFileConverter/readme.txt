there are 2 .cmd files

you may need to use notepad to edit those 2 files.

*run_all.cmd
this command will do:
1. label + seg + pos => crf
2. crf => split files (attributes + evaluations + expressions)

*run_crfsplit.cmd
this command will do:
1. crf => split files (attributes + evaluations + expressions)